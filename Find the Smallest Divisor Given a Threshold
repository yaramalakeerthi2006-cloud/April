from math import ceil 
class Solution:   
    def smallestDivisor(self, arr, k):
        
        for i in range(1,max(arr)+1):
            Sum = 0
            for num in arr:
                Sum+=ceil(num/i) 
            if(Sum<=k):
                return i 
